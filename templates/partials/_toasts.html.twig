{# Success Toast #}
{% macro success(message) %}
<div id="toast-success" class="fixed bottom-4 right-4 z-50 hidden group">
    <div class="relative bg-green-100 border border-green-200 text-green-900 rounded-md shadow-lg p-4 pr-8 min-w-[320px] animate-in fade-in-0 zoom-in-95">
        <div class="grid gap-1">
            <div class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span class="font-semibold text-sm">Succès</span>
            </div>
            <div class="text-sm text-green-800">{{ message }}</div>
        </div>
        <button onclick="this.parentElement.parentElement.classList.add('hidden')" class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2">
            <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
            <span class="sr-only">Fermer</span>
        </button>
    </div>
</div>
{% endmacro %}

{# Error Toast #}
{% macro error(message) %}
<div id="toast-error" class="fixed bottom-4 right-4 z-50 hidden group">
    <div class="relative bg-red-100 border border-red-200 text-red-900 rounded-md shadow-lg p-4 pr-8 min-w-[320px] animate-in fade-in-0 zoom-in-95">
        <div class="grid gap-1">
            <div class="flex items-center gap-2">
                <svg class="w-4 h-4 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
                <span class="font-semibold text-sm">Erreur</span>
            </div>
            <div class="text-sm text-red-800">{{ message }}</div>
        </div>
        <button onclick="this.parentElement.parentElement.classList.add('hidden')" class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2">
            <svg class="w-4 h-4 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
            <span class="sr-only">Fermer</span>
        </button>
    </div>
</div>
{% endmacro %}

{# Info Toast #}
{% macro info(message) %}
<div id="toast-info" class="fixed bottom-4 right-4 z-50 hidden group">
    <div class="relative bg-blue-100 border border-blue-200 text-blue-900 rounded-md shadow-lg p-4 pr-8 min-w-[320px] animate-in fade-in-0 zoom-in-95">
        <div class="grid gap-1">
            <div class="flex items-center gap-2">
                <svg class="w-4 h-4 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <span class="font-semibold text-sm">Information</span>
            </div>
            <div class="text-sm text-blue-800">{{ message }}</div>
        </div>
        <button onclick="this.parentElement.parentElement.classList.add('hidden')" class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2">
            <svg class="w-4 h-4 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
            <span class="sr-only">Fermer</span>
        </button>
    </div>
</div>
{% endmacro %}

{# Warning Toast #}
{% macro warning(message) %}
<div id="toast-warning" class="fixed bottom-4 right-4 z-50 hidden group">
    <div class="relative bg-yellow-100 border border-yellow-200 text-yellow-900 rounded-md shadow-lg p-4 pr-8 min-w-[320px] animate-in fade-in-0 zoom-in-95">
        <div class="grid gap-1">
            <div class="flex items-center gap-2">
                <svg class="w-4 h-4 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                </svg>
                <span class="font-semibold text-sm">Attention</span>
            </div>
            <div class="text-sm text-yellow-800">{{ message }}</div>
        </div>
        <button onclick="this.parentElement.parentElement.classList.add('hidden')" class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2">
            <svg class="w-4 h-4 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
            <span class="sr-only">Fermer</span>
        </button>
    </div>
</div>
{% endmacro %}